node_modules/
dist/
.env
data/
*.log
.DS_Store

# Heap snapshots (huge, scriptable via DIAGNOSTICS=1; redundant under data/
# but explicit so a future loosening of the data/ ignore doesn't leak them)
data/diagnostics/*.heapsnapshot
.claude/
*.bak
*.bak.*
*.backup*
.env.backup-*
